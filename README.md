# Sparky_KickStart_Demo

Sparky KickStart Demo

# Tests / Kickstart files:

* demo.ks

* auth_select_not_set.ks

* autopart-encrypted-1.ks

# Repository layout

- all kickstart files should be named as *.ks and placed under the root of a repository 

- if file is named `foo.ks`, then respected Sparrow test should be placed under `tasks/foo/` folder

- main.raku file does all the trick 

